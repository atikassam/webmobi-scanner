/**
 * Here we keep all the probable errors
 * response by this application(server)
 * to the client
 */
export enum Errors {
    BAD_REQUEST = 'Bad request'
    , INVALID_CREDENTIAL = 'Invalid credential'
}